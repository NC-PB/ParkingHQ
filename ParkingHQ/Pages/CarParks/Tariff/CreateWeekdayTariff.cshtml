@page
@model ParkingHQ.Web.Pages.CarParks.Tariff.CreateWeekdayTariffModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Daily Tariff</h4>
<hr />


<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                End Time
            </th>
            <th>
                Price
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.sections != null)
        {
            @foreach (var obj in Model.sections)
            {
                <tr>
                    <td width="20%">
                        @obj.TariffEndTime.TimeOfDay
                    </td>
                    <td width="20%">
                        @obj.TariffPrice
                    </td>
                    <td>
                        <div class="w-75 btnbtn-group" role="group">
                            <form asp-page-handler="view" method="post">
                                <input type="hidden" name="id" value="@obj.Id"/>
                                <input type="hidden" name="CarparkId" value="@Model.currentCarPark.Id" />
                                @*<a asp-page="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2"></a>*@
                                <button class="btn btn-danger"><i class="bi bi-trash-fill"></i> Delete</button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TariffSectionDaily.TariffEndTime" class="control-label"></label>
                <input asp-for="TariffSectionDailyOnlyTime" class="form-control" />
                <span asp-validation-for="TariffSectionDaily.TariffEndTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TariffSectionDaily.TariffPrice" class="control-label"></label>
                <input asp-for="TariffSectionDaily.TariffPrice" class="form-control" />
                <span asp-validation-for="TariffSectionDaily.TariffPrice" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-page="../Index" class="btn btn-secondary" style="width:150px">Back to List</a>
            </div>
        </form>
    </div>
</div>



<div>

</div>

<form asp-page-handler="button" method="post">
    <input hidden asp-for="currentCarPark.Id" />
    <input hidden asp-for="TariffSectionDaily.Id"/>
</form>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
